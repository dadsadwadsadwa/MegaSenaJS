/*
var mega = [];
while(mega.length < 6){
	var num = Math.ceil(Math.random()*10);
	while(mega.indexOf(num) != -1){
		num = Math.ceil(Math.random()*10);
	}
	mega.push(num);
}
document.write(mega);
//debugando vetores resul: comando prompt trata o valor como string portanto sempre vai retornar -1
var bilhete = [];
while(bilhete.length < 6){
	var num = Math.ceil(Math.random()*15);
	while(bilhete.indexOf(num) != -1){
		num = Math.ceil(Math.random()*15);
	}
	bilhete.push(num);
}
document.write(bilhete); //debugando consulta de valor inteiro dentro de um array 
var numero = parseInt(prompt());
if(bilhete.indexOf(numero) != -1){
	document.write("existe")
}
*/